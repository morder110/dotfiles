# NOTE: Due to this file being a source in multiple files, exec-once *might* be run multiple times if placed in this file.

# Hyprpaper timeouts/timers (in seconds)
$DISPLAY_TIMEOUT = 150
$LOCK_TIME = 300 # Display will dim after 30 seconds when locked if not already.
$SLEEP_WHEN_LOCKED_TIME = # 300
$SLEEP_THEN_HIBERNATE_TIME = # TODO: Set this up. This would replace the option above
$KEYBOARD_BACKLIGHT_TIMEOUT =

$LOCK_SCREEN_THEME = win95.conf
$LOCK_SCREEN_TRANSPARENCY = false
$LOCK_SCREEN_ANIMATION = false